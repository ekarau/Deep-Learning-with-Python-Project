# Bayram Selim Yılmaz — 2309011053

## Responsibilities

### Experiment 7 (Early Stopping)
- Implemented early stopping logic with configurable patience parameter inside train_model()
- Ran experiments with max 20 epochs and patience=5 for all four activation functions
- Showed that Sigmoid never triggers early stopping (runs all 20 epochs due to slow learning)
- Showed that ReLU stops at epoch 12 and LeakyReLU at epoch 13 — fast convergence leads to faster overfitting

### Bias-Variance Analysis
- Created the generalization gap visualization (train vs val loss with shaded area between them)
- Calculated and compared gaps: Sigmoid (0.107), Tanh (0.172), ReLU (0.167), LeakyReLU (0.215)
- Explained why Sigmoid's small gap is misleading (both losses are high = underfitting, not good generalization)
- Connected to bias-variance tradeoff from Week 2: high bias vs high variance interpretation

