# Vedat Efe Gezer — 229910158

## Responsibilities

### Model Architecture & Training Infrastructure
- Designed the base feedforward network (784→256→128→64→10) with flexible activation function injection
- Implemented the `build_model()` and `init_weights()` helper functions used by all experiments
- Wrote the `train_model()` function with support for schedulers, early stopping, and gradient clipping
- Implemented the `plot_results()` and `summary_table()` functions for consistent result visualization

### Experiment 2 (Activation × Optimizer)
- Configured and ran all 10 combinations: Sigmoid and ReLU each with SGD, SGD+Momentum, SGD+Nesterov, RMSProp, and Adam
- Analyzed why Sigmoid+SGD fails at 50.6% (vanishing gradient + constant learning rate)
- Documented how Adam's adaptive per-parameter learning rate partially compensates for Sigmoid's weak gradients
- Selected Adam as the default optimizer for remaining experiments based on these results
